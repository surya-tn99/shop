<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product</title>
    <% for(let cssPath of cssPaths) { %>
    <link rel="stylesheet" href=<%= cssPath %> >    
    <% } %>

</head>
<body>

    <%- include("include/nav") %>

    <% if(active === "product-detail") { %>

        <%- include("include/product-detail") %>

    <% } else { %>
        
        <%- include("include/all-products") %>
        
    <% } %>
    <br>
    <br>
    <br>
    <br>
    <br>
</body>
</html>